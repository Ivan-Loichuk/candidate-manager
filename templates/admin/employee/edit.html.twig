{% extends 'admin/employee/index.html.twig' %}

{% block body %}
    <div class="content">
        {{ form_start(form) }}
        <div class="container-fluid">
            <div class="row">
                <div class="col-md-12">
                    {% include '@common_admin_tpl/flashes.html.twig' %}
                </div>
            </div>

            <div class="row">
                <div class="col-md-6">
                    {% include '@admin_employee_tpl/_partial/form/main-section.html.twig' %}
                </div>
                <div class="col-md-6">
                    {% include '@admin_employee_tpl/_partial/form/contact-section.html.twig' %}
                </div>
                <div class="col-md-6">
                    {% include '@admin_employee_tpl/_partial/form/passport-section.html.twig' %}
                </div>

                <div class="col-md-6">
                    <div class="row">
                        <div class="col-md-12">
                            <div class="text-right">
                                {{ form_widget(form.update_profile,  { 'label': "Zapisz"|trans }) }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock  %}