<div class="card border-top">
    <div class="card-header">
        <h4 class="card-title">{{ 'Umowy'|trans }}</h4>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-lg-12 col-md-12 col-sm-12">
                {% macro tagCollectionItem(formData) %}
                    <div class="col-lg-6 col-md-6 col-sm-6">
                        <div class="form-group">
                            <label for="contract_type">{{ 'rodzaj umowy'|trans|capitalize }}</label>
                            {{ form_widget(formData.type) }}
                            <div class="errors">
                                {{ form_errors(formData.type) }}
                            </div>
                        </div>
                    </div>
                    <div class="w-100"></div>
                    <div class="col-lg-6 col-md-6 col-sm-6">
                        <div class="form-group">
                            <label for="contract_data_from">{{ 'data od'|trans|capitalize }}</label>
                            {{ form_widget(formData.dateFrom) }}
                            <div class="errors">
                                {{ form_errors(formData.dateFrom) }}
                            </div>
                        </div>
                    </div>
                    <div class="col-lg-6 col-md-6 col-sm-6 remove-collection-wrapper">
                        <div class="form-group w-100">
                            <label for="contract_date_to">{{ 'data do'|trans|capitalize }}</label>
                            {{ form_widget(formData.dateTo) }}
                            <div class="errors">
                                {{ form_errors(formData.dateTo) }}
                            </div>
                        </div>
                        <a href="#" class="js-remove-collection pull-right">
                            <i class="fas fa-times"></i>
                        </a>
                    </div>
                {% endmacro %}

                <div id="contract-fields-list" class="list-group"
                     data-prototype="{{ _self.tagCollectionItem(form.contract.vars.prototype)|e }}"
                     data-widget-tags="{{ '<div class="row js-collection"></div>'|e }}"
                     data-widget-counter="{{ form.contract|length }}">
                    {% if form.contract|length > 0 %}
                        {% for contractField in form.contract %}
                            <div class="row js-collection">
                                <div class="col-lg-6 col-md-6 col-sm-6">
                                    <div class="form-group">
                                        <label for="contract_type">{{ 'rodzaj umowy'|trans|capitalize }}</label>
                                        {{ form_widget(contractField.type) }}
                                        <div class="errors">
                                            {{ form_errors(contractField.type) }}
                                        </div>
                                    </div>
                                </div>
                                <div class="w-100"></div>
                                <div class="col-lg-6 col-md-6 col-sm-6">
                                    <div class="form-group">
                                        <label for="contract_date_from">{{ 'data od'|trans|capitalize }}</label>
                                        {{ form_widget(contractField.dateFrom) }}
                                        <div class="errors">
                                            {{ form_errors(contractField.dateFrom) }}
                                        </div>
                                    </div>
                                </div>
                                <div class="col-lg-6 col-md-6 col-sm-6 remove-collection-wrapper">
                                    <div class="form-group w-100">
                                        <label for="contract_date_to">{{ 'data do'|trans|capitalize }}</label>
                                        {{ form_widget(contractField.dateTo) }}
                                        <div class="errors">
                                            {{ form_errors(contractField.dateTo) }}
                                        </div>
                                    </div>
                                    <a href="#" class="js-remove-collection pull-right">
                                        <i class="fas fa-times"></i>
                                    </a>
                                </div>
                            </div>
                        {% endfor %}
                    {% else %}
                        {{ form_widget(form.contract) }}
                    {% endif %}
                </div>

                <a type="button"
                   class="add-another-collection-widget"
                   data-list-selector="#contract-fields-list">{{ 'Dodaj umowę'|trans }}</a>

            </div>
        </div>
        <div class="clearfix"></div>
    </div>
</div>