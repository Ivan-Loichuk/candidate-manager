<div class="card border-top">
    <div class="card-header">
        <h4 class="card-title">{{ 'badania'|trans|capitalize }}</h4>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-lg-12 col-md-12 col-sm-12">
                {% macro tagCollectionItem(formData) %}
                    <div class="col-lg-6 col-md-6 col-sm-6">
                        <div class="form-group">
                            <label for="candidate_data_from">{{ 'data od'|trans|capitalize }}</label>
                            {{ form_widget(formData.dateFrom) }}
                            <div class="errors">
                                {{ form_errors(formData.dateFrom) }}
                            </div>
                        </div>
                    </div>
                    <div class="col-lg-6 col-md-6 col-sm-6 remove-collection-wrapper">
                        <div class="form-group w-100">
                            <label for="candidate_date_to">{{ 'data do'|trans|capitalize }}</label>
                            {{ form_widget(formData.dateTo) }}
                            <div class="errors">
                                {{ form_errors(formData.dateTo) }}
                            </div>
                        </div>
                        <a href="#" class="js-remove-collection pull-right">
                            <i class="fas fa-times"></i>
                        </a>
                    </div>
                {% endmacro %}

                <div id="medical-examination-fields-list" class="list-group"
                     data-prototype="{{ _self.tagCollectionItem(form.medicalExamination.vars.prototype)|e }}"
                     data-widget-tags="{{ '<div class="row js-collection"></div>'|e }}"
                     data-widget-counter="{{ form.medicalExamination|length }}">
                    {% if form.medicalExamination|length > 0 %}
                        {% for medicalExaminationField in form.medicalExamination %}
                            <div class="row js-collection">
                                <div class="col-lg-6 col-md-6 col-sm-6">
                                    <div class="form-group">
                                        <label for="candidate_date_from">{{ 'data od'|trans|capitalize }}</label>
                                        {{ form_widget(medicalExaminationField.dateFrom) }}
                                        <div class="errors">
                                            {{ form_errors(medicalExaminationField.dateFrom) }}
                                        </div>
                                    </div>
                                </div>
                                <div class="col-lg-6 col-md-6 col-sm-6 remove-collection-wrapper">
                                    <div class="form-group w-100">
                                        <label for="candidate_date_to">{{ 'data do'|trans|capitalize }}</label>
                                        {{ form_widget(medicalExaminationField.dateTo) }}
                                        <div class="errors">
                                            {{ form_errors(medicalExaminationField.dateTo) }}
                                        </div>
                                    </div>
                                    <a href="#" class="js-remove-collection pull-right">
                                        <i class="fas fa-times"></i>
                                    </a>
                                </div>
                            </div>
                        {% endfor %}
                    {% else %}
                        {{ form_widget(form.medicalExamination) }}
                    {% endif %}
                </div>

                <a type="button"
                   class="add-another-collection-widget"
                   data-list-selector="#medical-examination-fields-list">{{ 'dodaj okres badania'|trans|capitalize }}</a>

            </div>
        </div>
        <div class="clearfix"></div>
    </div>
</div>